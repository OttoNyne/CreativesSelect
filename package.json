{
  "name": "creativesselect",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "set \"PATH=C:\\Program Files\\nodejs;%PATH%\" && concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
